const modelBox = document.querySelector('.add-container');
       var btn = document.querySelector(".add-items");
     btn.addEventListener('click', () => {
        modelBox.classList.add("active")
     });